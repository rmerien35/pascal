This is a Pascal compiler in Java for studies purposes.

gram_pascal.dat is the grammar of the language use by gbf program (grammar compiler) to compile into tab.dat (binary version) use by the Pascal compiler via the Loader class.

Use the main class to compile from prgs/*.pas to obj files (binary) you can run with the imp "Interpreteur pour Machine à Pile" (stack machine interpretor in english).
